apply from: '../app.gradle'
apply plugin: 'org.jetbrains.kotlin.android'

android {
    resourcePrefix 'store_'
    defaultConfig {
        if (!isRelease) {
            applicationId 'com.github.jeffery.store'
            versionCode 1
            versionName '1.0'
        }
    }
}
dependencies {
}
